<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_inherit_discount" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.discount</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
<!--             Use Xpath -->
            <xpath expr="//sheet//div[@name='button_box']" position="inside">
                <button name="change_pricelist" type="object" class="oe_stat_button" icon="fa-money" attrs="{'invisible':['|',('is_new_customer','=', False),('type_name','!=','Quotation')]}">
                    First purchase
                    <field name="is_new_customer" invisible="1"/>
                    <field name="type_name" invisible="1"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>